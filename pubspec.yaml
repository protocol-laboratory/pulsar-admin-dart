name: pulsar_admin
description: A HTTP client for interacting with the Apache Pulsar REST API
version: 0.0.1

environment:
  sdk: '>=2.18.5 <3.0.0'

dependencies:
  http: ^0.13.5

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
